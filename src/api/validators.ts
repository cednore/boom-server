/**
 * Validate an emit request.
 *
 * @param {any} body Request body to validate
 * @returns {boolean} Whether valid or not
 */
export function validateEmitRequest(body): boolean {
    // TODO: Validate emit request body
    return true;
}

/**
 * Validate an join request.
 *
 * @param {any} body Request body to validate
 * @returns {boolean} Whether valid or not
 */
export function validateJoinRequest(body): boolean {
    // TODO: Validate join request body
    return true;
}

/**
 * Validate an leave request.
 *
 * @param {any} body Request body to validate
 * @returns {boolean} Whether valid or not
 */
export function validateLeaveRequest(body): boolean {
    // TODO: Validate leave request body
    return true;
}
